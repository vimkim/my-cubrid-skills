# Independent forward review: CUBRID CI repair

Evaluated source skills and references without live API calls, builds, commits, pushes or skill edits. Scenarios are synthetic; conclusions establish instruction behavior, not runtime CI correctness. Paths below are relative to `/home/vimkim/gh/my-cubrid-skills` unless stated otherwise.

## Scenario outcomes

1. **Current engine A, known shell path, local TC B, CI per-file TC C, no fix approval.** Next action: pin PR A and the private repository identity; inspect tracked path and C Git objects, compare answers/fixtures against B and dirty changes, and use an isolated checkout if needed. Analyze the evidence and write a concrete fix proposal; do not modify source/test files or trigger CI. Ledger: occurrence `observed`, then `analyzed`/`awaiting-fix-approval`; unproven C identity stays an attribution limitation. Governing passages: `cubrid-ci-fix/SKILL.md:22` requires repository/revision/full-path resolution, `:30` leaves source/test files unchanged until approval, and `references/ci-evidence.md`, “Map each TC to its repository,” prefers per-file source records and requires exact evidence comparison. No assumption that engine SHA equals TC SHA or that local B reproduces C.

2. **Engine-only hypothesis H approved, patch exists, CTP exit 0 but total/success/fail all 0.** Next action: inspect scenario selection, exclusions, setup and emitted logs; correct execution configuration and rerun the intended case within the approved scope. Do not request push approval, edit test answers, or mark local verification passed. Ledger: preserve the zero-case attempt as inconclusive, retain `fixing` (or `blocked` with the concrete unavailable prerequisite), and retain H's approval without extending it. Governing passages: `cubrid-sql-run/SKILL.md:35` requires positive execution count and explicitly rejects zero-case success; `cubrid-ci-fix/SKILL.md:32` rejects scope expansion and `:51` requires evidence/hypothesis change before retry.

3. **Engine/public-TC pushes and one /run all approved; engine push succeeds, TC push fails; absent shell status with same-head trigger receipt.** Next action: reconcile both actual remote branches and save the successful engine push; diagnose the failed TC publication and retry only the unchanged approved publication if still applicable. Inspect the existing trigger's job/pipeline handle; do not infer that shell never started or post a duplicate. If the existing run captured old TC content, record that mismatch and seek explicit same-head retrigger authority unless the earlier grant expressly covered that retry. Ledger: engine publication receipt retained; TC publication incomplete; effort `pushed-awaiting-ci` with an outstanding publication/selection problem, never `remote-passed`. Governing passages: `cubrid-ci-fix/SKILL.md:59` requires partial-failure reconciliation, `:63` requires specific same-head retry authorization, `:67` addresses unpublished TC changes; `cubrid-ci-trigger/SKILL.md:60` keeps the known receipt relevant despite missing checks. A generic allowance for one trigger is not evidence that an additional trigger remains authorized after a saved current-head trigger.

4. **Old-head CI pass, moved PR head, normalized failure lists empty but raw errors and prerequisite failure.** Next action: mark the old-head snapshot superseded, collect new-head check identities, inventory raw abnormal tests and prerequisite job failure, and preserve missing/unavailable coverage. Ledger: old snapshot `superseded`; new errors/job failures `observed` and unresolved; no green PR claim. Governing passages: `cubrid-ci-fix/SKILL.md:24` requires every abnormal/job failure, `:65` forbids mixing moved-head evidence, `:69` requires current-head required checks; `references/ci-evidence.md` first paragraph explicitly states normalized failure lists exclude errors/unknowns.

5. **Standalone trigger with unpushed local commits.** Next action: resolve remote PR head and compare it with local HEAD; tell the user exactly which revision would run. Do not push or commit. If the request makes remote-head testing explicit, continue duplicate checks and the authorized comment; otherwise clarify whether testing the existing pushed head is intended. Ledger: standalone trigger need not create a repair ledger; retain a pending target decision or, after posting, a head/comment receipt. Governing passages: `cubrid-ci-trigger/SKILL.md:43` says trigger authorization does not authorize a push and permits testing the identified already-pushed head when intended; `:60` retains duplicate protection.

## Medium replay fidelity

The new SQL skill routes medium correctly in the inspected local CTP implementation, provided actual configuration, dataset and child binary identities are verified as instructed. `cubrid-sql-run/SKILL.md:16-24` preserves the medium suite, intact case checkout, matching `data_file`, setup and relevant settings. Its positive-count/case-result checks prevent a mere exit-zero claim.

Read-only source evidence from `/home/vimkim/gh/ctp/cubrid-testtools/CTP`: `sql/bin/run.sh:152-153` accepts a scenario file; `:237-238` chooses `mdb` for medium; `:504-505` calls `make_db_data mdb`; `:634-654` validates the dataset path; `:658-666` copies/extracts and loads schema/index/object fixtures. `conf/medium.conf` sets `test_category=medium` and points `data_file` at `medium/files/mdb.tar.gz`. The helper expects the copied archive basename `mdb.tar.gz` (hard-coded extraction at `run.sh:660`); a differently named equivalent archive would need attention. The skill's requirement to inspect the actual runner and fixture/setup logs covers this, so it is an operational caveat rather than a demonstrated instruction defect.

## Defects

No concrete defect demonstrated by these five scenario evaluations. The rules consistently preserve both human approval gates, testcase revision identity, partial publication history, nonzero execution proof and current-head CI completion. This is a read-only behavioral/source review; no claim is made that a real medium case or live CI run has passed.
